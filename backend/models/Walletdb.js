/*const walletdb = require("mongoose");
const walletdbschema = new walletdb.Schema({
  walletAmount: {
    required: true,
    type: Number,
  },
  Date: {
    required: true,
    type: Date,
  },
});
const wallet = walletdb.model("wallet", walletdbschema);
module.exports = wallet;*/
