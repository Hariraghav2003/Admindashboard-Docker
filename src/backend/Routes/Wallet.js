/*const express = require("express");
const router = express.Router();
const {
  Walletamountupdation,
  walletAmount,
} = require("../controller/Walletcontroller");
router.get("/getwalletdetails", walletAmount);
router.post("/addwalletamount", Walletamountupdation);
module.exports = router;  */
